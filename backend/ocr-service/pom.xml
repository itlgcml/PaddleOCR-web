<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.paddleocr</groupId>
        <artifactId>paddle-ocr-backend</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>ocr-service</artifactId>
    <name>ocr-service</name>
    <description>业务层：识别/历史记录业务编排、事务边界、出参 VO</description>

    <dependencies>
        <dependency>
            <groupId>com.paddleocr</groupId>
            <artifactId>ocr-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.paddleocr</groupId>
            <artifactId>ocr-client</artifactId>
        </dependency>
        <dependency>
            <groupId>com.paddleocr</groupId>
            <artifactId>ocr-dal</artifactId>
        </dependency>
        <!-- @Service / @Transactional（spring-tx 由 mybatis-plus starter 传递） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- OcrService 接口签名使用 MultipartFile（后续可选优化去掉） -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- DTO 入参校验注解（javax.validation，注解本身无运行时依赖；校验执行在 ocr-web 的 validation starter） -->
        <dependency>
            <groupId>javax.validation</groupId>
            <artifactId>validation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
        <!-- 登出 Token 吊销黑名单（Lettuce 客户端，仅 KV 存在性语义，不承载会话） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <!-- PasswordSupport 编译所需（仅 crypto 子包，ocr-service 不依赖 security starter） -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <!-- JWT 签发与解析（HS256，claims 含 jti/type） -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>
</project>
